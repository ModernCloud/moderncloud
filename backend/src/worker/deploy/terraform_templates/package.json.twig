{
  "name": "project",
  "version": "1.0.0",
  "dependencies": {
    {% for package in packages %}
      "{{package.name}}": "{{package.version}}"{% if loop.last == false %},{% endif %}
    {% endfor %}
  }
}
